#include <iostream>
using namespace std;
int main()
{
   int rows,i,j,k=1;
   cout<<"Enter the no of rows:";
   cin>>rows;
   for(i=1;i<=rows;i++)
   {
       for(j=rows;j>=i;j--)
       {
           cout<<k<<"  ";
           k++;
       }
       cout<<"\n";
   }

    return 0;
}
