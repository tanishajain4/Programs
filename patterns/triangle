#include<iostream>
using namespace std;
int main()
{int i,j,rows,temp;
cout<<"Enter the number of rows:";
cin>>rows;
for(i=0;i<rows;i++)
{cout<<i+1<<"\t";
   temp=i+1;
   j=i;
   for(int k=6;k>0;k--)
     {if(j)
     {temp=temp+k;
      cout<<temp<<"\t"; j--;}
     }
     cout<<"\n";
}
return 0;
}
