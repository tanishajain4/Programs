#include <iostream>
using namespace std;
int main()
{
    int rows,i,j,k=11;
    cout<<"Enter the number of rows:";
    cin>>rows;
    for(i=1;i<=rows;i++)
    {
        for(j=1;j<=i;j++)
      {
          cout<<k<<" ";
          k++;
      }
      k=k-i+1;
      cout<<"\n";
    }
    return 0;
}
